import {Screen, Text} from '@components';
import {AppScreenProps} from '@routes';

export function FavoriteDetailsListScreen({}: AppScreenProps<'FavoriteDetailsListScreen'>) {
  return (
    <Screen canGoBack>
      <Text />
    </Screen>
  );
}
